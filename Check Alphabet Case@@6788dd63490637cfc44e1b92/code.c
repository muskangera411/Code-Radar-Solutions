#include <stdio.h>
#include <ctype.h>
int main() {
    char a;
    printf("");
    scanf("%c",&a);
    if(isupper(c)){printf("Uppercase");}
    else if(islower(c)){printf("Lowercase");}
    else{printf("Not an alphabet");}

    return 0;
}