#include <stdio.h>
#include <string.h>
int main(){
    char a[15];b[15];
    fgets(str,sizeof(a),stdin);
    fgets(str,sizeof(b),stdin);
    strcat(a,b);
    printf("%s%s",a,b);
    return 0;

}